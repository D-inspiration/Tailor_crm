# Flask
SECRET_KEY=change-me-in-production
DATABASE_URL=sqlite:///sbeae.db

# Session
SESSION_TTL_HOURS=24
SESSION_MAX_IDLE_MINUTES=30

# Risk
RISK_THROTTLE_THRESHOLD=6
RISK_BLOCK_THRESHOLD=10
RISK_BURST_WINDOW_SECONDS=10
RISK_BURST_MAX_EVENTS=20

# Fingerprint
FINGERPRINT_TRUST_DECAY=0.05
FINGERPRINT_REUSE_WINDOW_HOURS=1
FINGERPRINT_REUSE_MAX_USERS=1

# Django integration
DJANGO_SHARED_SECRET=django-flask-shared-secret

# Logging
LOG_LEVEL=INFO
